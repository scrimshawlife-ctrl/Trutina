# T04B runtime slice

Implements only the registered DECOMPOSE methods `MURPHY_EXACT_V1` and `MURPHY_BINNED_V1` against the validated BATCH cohort contract.

T05 uncertainty/resampling and all other calibration diagnostics remain outside this slice.
