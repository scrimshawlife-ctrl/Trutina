# Clarify — Trutina 000

C1 T0 is not a trained net.
C2 Old Notion Brier pages and `specs/000-brier-spine` stay derivative, not deleted.
C3 Hyperlex and Athanor cannot SCORE. Their packets keep `brier: null` / `efficacy: null`.
C4 First live mode is SCORE. Other modes stub NOT_COMPUTABLE until a later cycle opens them in this repo.
C5 Home is `yggdrasil.replay`.
C6 Fusion does not mutate weights.
C7 Specialist id stays `abx.brier`. Display is Trutina. Function is `trutina.score`.
C8 Python package may stay `brier` until a rename cycle.
C9 This repo does not finalize ledger ids. SHADOW may compute a score and still leave `ledger_id` null.
C10 HollerSports may consume scores. It does not wager through this specialist.
